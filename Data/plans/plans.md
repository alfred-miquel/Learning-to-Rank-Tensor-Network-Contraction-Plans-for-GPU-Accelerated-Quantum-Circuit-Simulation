Two files, plans_part_aa and plans_part_ab, were created by splitting file plans.tar.gz because of memory limitations.You need to join them using the command in Linux:
cat plans_part_* > plans.tar.gz

All seven plans and circuits used in the article are detailed in this compressed file

Seven uncompressed files are added to test a QFT circuit with 5 qubits.
